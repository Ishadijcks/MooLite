import { ChatIconHrid } from "src/MooLite/core/chat/ChatIconHrid";

export interface CharacterChatIcon {
    // TODO(@Isha): This is probably the list of chat icons that the player has bought.
    //  Since I do not have any, I can't check the interface, so let's guess it's this and not `chatIconHrid`
    hrid: ChatIconHrid;
}
